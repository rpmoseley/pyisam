# Build the binaries that are part of vbisam
vbcheck = executable('vbcheck', files(['vbcheck.c', 'vbisam.h']), include_directories: vbisam_incl, dependencies: [vbisam])
vbrecover = executable('vbrecover', files(['vbrecover.c', 'vbisam.h']), include_directories: vbisam_incl, dependencies: [vbisam])
